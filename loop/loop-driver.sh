#!/usr/bin/env bash
set -euo pipefail
# Filled by a later issue: W2-3 (the turn driver). STUB only.
# Runs one loop turn: kill-switch check → handoff (worktree + worker) → verification
# (different-model reviewer) → persistence (heartbeat/progress/round) → honor halts.
echo "loop-driver: TODO — the turn driver is implemented by a later issue (W2-3)."
exit 0
