<?xml version="1.0" encoding="UTF-8"?>
<!--
  Filled by a later issue: the scheduling-recipe issue. STUB only.
  This is a commented placeholder launchd agent that will run loop/loop-driver.sh on a
  cadence. It intentionally defines no real job yet so it cannot schedule anything.
-->
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<plist version="1.0">
  <dict>
    <!-- TODO (scheduling issue): Label, ProgramArguments (loop/loop-driver.sh),
         StartCalendarInterval, EnvironmentVariables (PI_OFFLINE=1,
         PI_SKIP_VERSION_CHECK=1), StandardOutPath/StandardErrorPath. -->
  </dict>
</plist>
