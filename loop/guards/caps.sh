#!/usr/bin/env bash
set -euo pipefail
# Filled by a later issue: the guards issue. STUB only.
# Enforces the daily turn cap and per-turn token accounting (token-blowout guard).
echo "guards/caps: TODO — caps guard is implemented by a later issue."
exit 0
