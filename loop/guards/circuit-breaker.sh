#!/usr/bin/env bash
set -euo pipefail
# Filled by a later issue: the guards issue. STUB only.
# Halts the loop after a consecutive-failure ceiling is reached (one of three independent halts).
echo "guards/circuit-breaker: TODO — circuit breaker is implemented by a later issue."
exit 0
