#!/usr/bin/env bash
set -euo pipefail
# Filled by a later issue: the state-helpers issue. STUB only.
# Reads/writes loop/state/* (heartbeat.json, progress.md, artifacts/round-NNN.json)
# under flock so concurrent turns never corrupt state.
echo "state/helpers: TODO — state read/write helpers are implemented by a later issue."
exit 0
