#!/usr/bin/env bash
set -euo pipefail
# Filled by a later issue: the handoff issue. STUB only.
# Wraps plain `git worktree` to create/remove an isolated worktree + branch per finding
# (branch feat/lk-nnnn, worktree .worktrees/feat-lk-nnnn).
echo "worktree: TODO — the git-worktree wrapper is implemented by a later issue."
exit 0
