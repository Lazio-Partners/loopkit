#!/usr/bin/env node
// loopkit-readiness — L0–L3 readiness audit (STUB, W0-0).
//
// The audit logic (L0–L3 checks) is a downstream issue. W0-0 ships only this stub
// so the `loopkit-readiness` bin name is reserved and `npm link` works without a
// missing-file crash. It prints a notice and exits non-zero on purpose.
console.error("loopkit-readiness: not yet implemented — see the readiness-audit issue (Wx).");
process.exit(1);
