#!/usr/bin/env node
// Filled by a later issue: the registry-validation issue. STUB only.
// Will validate patterns/registry.yaml against patterns/registry.schema.json (join-key
// uniqueness, driver-only status transitions). For now it is a no-op that exits 0.
console.log("validate-registry: TODO -- registry validation is implemented by a later issue.");
process.exit(0);
