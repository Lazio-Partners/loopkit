<!-- Filled by a later issue: the verification issue authors the verifier body. STUB: valid frontmatter + TODO body. -->
---
name: loop-verifier
description: Skeptical reviewer skill — grade a worker's output against a finding's acceptance criteria and emit a verdict. Invoked by reading this SKILL.md or via /skill:loop-verifier (there is no Skill tool in pi).
---

# loop-verifier (stub)

TODO: author the skeptical-review procedure — run the deterministic floor (tests/baseline), inspect the diff, and produce a verdict conforming to `loop/state/schema/verdict.schema.json`. The reviewer model MUST differ from the worker model.
