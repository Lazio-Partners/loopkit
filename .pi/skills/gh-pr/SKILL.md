<!-- Filled by a later issue: the connectors issue authors the body. STUB: valid frontmatter + TODO body. -->
---
name: gh-pr
description: Open and inspect GitHub pull requests via the gh CLI (a CLI tool documented in a skill — loopkit ships no MCP server). loopkit opens PRs but never auto-merges.
---

# gh-pr (stub)

TODO: document the `gh pr` commands the loop uses (create, view, checks). Opening a PR is the end of an approve verdict — a human merges, never the loop.
